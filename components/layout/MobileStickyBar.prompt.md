Chrome de conversão do mobile (<640px). Reserve espaço no `body` com `padding-bottom: var(--mobile-sticky-bar-height)`.

```jsx
<MobileStickyBar whatsappHref={waHref("Preciso de atendimento urgente. Pode me ajudar?")} />
```
