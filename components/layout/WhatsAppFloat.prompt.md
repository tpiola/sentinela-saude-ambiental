Único elemento circular do site, e o único uso do verde brilhante #25D366 (o CTA sólido de seção usa `--brand-whatsapp`).

```jsx
<WhatsAppFloat href={waHref("Preciso de atendimento urgente.")} />
```

Aparece só em ≥640px; no mobile o lugar dele é a `MobileStickyBar`.
