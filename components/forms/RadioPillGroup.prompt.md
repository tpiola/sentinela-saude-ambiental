Urgência do atendimento — três opções, nunca mais.

```jsx
<RadioPillGroup name="urgencia" legend="Quando precisa do atendimento?"
  options={["Hoje","Nesta semana","Quero orientação"]} value={form.urgencia} onChange={v=>set("urgencia",v)} />
```
