Set autoral de pragas do site (escorpião, barata, rato, formiga, aranha, mosquito, mosca, cupim, caixa d'água) — desenho linear com preenchimento translúcido, viewBox 24×28.

```jsx
<PestIcon name="escorpiao" size={28} style={{ color: "var(--brand-navy)" }} />
```

Sempre em `var(--brand-navy)` sobre fundo claro. Não substitua por emoji nas superfícies novas.
